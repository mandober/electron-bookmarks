import curry from './curry.js'

// konst :: a -> b -> a
const konst = curry((a, _) => a);

//---------------------------------------------------------------------- export
export default konst;


// McConstant, McConst, McKonst, McConstantine
